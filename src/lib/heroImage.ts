// Image de fond du hero (page d'accueil) — générée via Canva, hébergée sur
// Supabase Storage une fois choisie et validée par le fondateur. Tant que ces
// valeurs sont null, HeroScene retombe proprement sur le dégradé animé
// existant (aucun espace vide ou cassé).
export const HERO_IMAGE_JPG_URL: string | null = null;
export const HERO_IMAGE_WEBP_URL: string | null = null;
